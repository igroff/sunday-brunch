doctype html
html(lang="en")
  head
    title= pageTitle
    link(rel="stylesheet" media="screen" href= appCss)
    link(rel="stylesheet" media="screen" href= vendorCss)
    meta(charset="utf-8")
    meta(http-equiv="X-UA-Compatible" content="IE=edge")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    script(type='text/javascript' src= appJs)
    script(type='text/javascript' src= vendorJs)
    script(type='text/javascript').
      var base = require('base');
      var coffee_base = require('coffee_base');
      angular.module('theApp', []).
        controller('ValidationController', function($scope){
          $scope.baseIsObject = _.isObject(base);
          $scope.coffeeBaseIsObject = _.isObject(coffee_base);
          $scope.weHaveJQuery = _.isFunction($);
          $scope.weHaveMoment = _.isObject(moment);
          $scope.weHaveLodash = false || _;
          $scope.weHaveAngular = angular;
          $scope.vendorCoffeeFiles = false || window.vendorBaseCoffee;
        });
    style
      | .good { color: green; }
      | .startOffBad { color: red; }
      | a { text-decoration: none; color: inherit; }
      | a:hover { text-decoration: underline; }
      | .validations { list-style: none; }
  body(ng-app='theApp' ng-controller='ValidationController')
    h1
      |I'm generated by a 
      a(href='http://jade-lang.com/') Jade
      |  template
    p
      | I was generated by the 
      a(href='https://github.com/ilkosta/static-jade-brunch') static-jade-brunch 
      | plugin.  It's currently configured to compile items within the
      | app directory named *.static_jade.  Once compiled to *.html they will
      | be moved into the assets directory for normal handling by brunch. 
    p
      | Things I found
      ul(class='validations startOffBad')
        li(ng-class="{good: baseIsObject}") app/*.js files importing
        li(ng-class="{good: coffeeBaseIsObject}") app/*.coffee files importing
        li(ng-class="{good: vendorCoffeeFiles}") vendor/*.coffee files importing
        li(ng-class="{good: weHaveJQuery}")
          a(href="http://api.jquery.com") JQuery
        li(ng-class="{good: weHaveMoment}") 
          a(href="http://momentjs.com/") Moment.js
        li(ng-class="{good: weHaveLodash}")
          a(href="http://lodash.com/") lodash
        li(ng-class="{good: weHaveAngular}")
          a(href="http://angularjs.org")  Angular
          |  ( but then nothing would work without Angular )
